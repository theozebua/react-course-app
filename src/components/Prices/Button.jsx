export default function Button({ price }) {
  return <button className='rounded-full border border-primary bg-primary px-10 py-2 font-semibold uppercase tracking-wider text-white shadow-lg shadow-primary/50'>{price}</button>
}
